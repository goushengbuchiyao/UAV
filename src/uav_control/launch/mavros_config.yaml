plugin_blacklist: []
plugin_whitelist: [distance_sensor, px4flow, rangefinder, lidar]

# 如果只想启用 lidar/flow，也可以用 whitelist，例如：
# plugin_whitelist: [distance_sensor, px4flow, rangefinder]

# 其他参数保持默认
sys:
  heartbeat_rate: 1.0
  conn_timeout: 30.0
  sysid: 1
  component_id: 240
